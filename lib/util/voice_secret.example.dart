/// 阿里云 NLS 语音识别密钥示例文件
///
/// 使用说明：
/// 1. 将本文件复制并重命名为 voice_secret.dart（与 example 同目录）
/// 2. 将下方各字段替换为真实的阿里云密钥
/// 3. voice_secret.dart 已被 .gitignore 排除，不会提交到版本库
class VoiceSecret {
  /// 语音项目 AppKey
  static const String appKey = 'your-app-key';

  /// 阿里云 AccessKey ID
  static const String accessKeyId = 'your-access-key-id';

  /// 阿里云 AccessKey Secret
  static const String accessKeySecret = 'your-access-key-secret';
}
